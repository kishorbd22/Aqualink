/**
 * Aqualink - Pages
 *
 * Page-level components / routes (Next.js Pages Router fallback).
 */

export default function WelcomePage() {
  return (
    <div style={{ textAlign: 'center', marginTop: '3rem' }}>
      <h1>Welcome to AquaLink</h1>
      <p>Your full-stack application is ready.</p>
    </div>
  );
}